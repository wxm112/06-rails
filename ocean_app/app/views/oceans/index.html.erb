<h1>Oceans</h1>

<ul>
    <% @oceans.each do |ocean| %>
    <li>
        <%= link_to(ocean.name, ocean_path(ocean.id)) %> has <%= pluralize(number_to_human(ocean.area), 'square meter') %>
    </li>
    <% end %>
</ul>